node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
.cache/
ui/node_modules/
ui/dist/
public/cloudflare-worker.js
# A fetched roster is personal account data, and every test takes one as an
# argument rather than reading a committed copy.
player.json
# Screenshots the UI smoke test writes beside itself.
ui-*.png
